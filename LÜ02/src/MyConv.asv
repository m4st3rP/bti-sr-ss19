function Y = MyConv(s_Vek, h_Vek)
    m = length(s_Vek);
    n = length(h_Vek);
    k = m + n - 1;
    j = max(1, k+1-n):min(k,m);
    Y = zeros(k);
    
    for result_index=1:k
        Y(result_index) = sum(
    X = [s_Vek, zeros(1,n)];
    H = [h_Vek, zeros(1,m)];
    for i = 1:n+m-1
        Y(i) = 0;
        for j=1:m
            if(i-j+1>0)
                Y(i)=Y(i)+X(j)*H(i-j+1);
            end
        end
    end
return